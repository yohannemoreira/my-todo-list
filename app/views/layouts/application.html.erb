<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "My Todo List" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 <%= 'pb-16' unless controller_name == 'sessions' || controller_name == 'passwords' %>">
    <% if Current.user && controller_name != 'sessions' && controller_name != 'passwords' %>
      <header class="bg-white shadow-sm border-b border-gray-200">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4 flex justify-between items-center">
          <div class="flex items-center gap-4">
            <%= link_to root_path, class: "font-semibold text-gray-900 hover:text-blue-600" do %>
              <%= t('app.name') %>
            <% end %>
          </div>
          <div class="flex items-center gap-4">
            <span class="text-sm text-gray-600"><%= Current.user.email_address %></span>
            <%= button_to t('auth.sessions.logout_button'), session_path, method: :delete, class: "text-sm text-red-600 hover:text-red-700 font-medium" %>
          </div>
        </div>
      </header>
    <% end %>
    
    <%= yield %>
    
    <% unless controller_name == 'sessions' || controller_name == 'passwords' %>
      <%= render 'shared/delete_modal' %>
      <%= render 'shared/footer' %>
    <% end %>
  </body>
</html>
